---
"@traffical/cli": minor
---

Add event schema support: property definitions in traffical.yaml with JSON Schema-based validation, property groups for reusable schemas, `typegen` command for generating typed event interfaces (TypeScript), and `--include-types` flag on `pull` command.
