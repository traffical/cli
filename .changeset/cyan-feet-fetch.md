---
"@traffical/cli": minor
---

Add namespace-as-key-prefix support: parameters are now grouped by namespace in config files, with local keys under a namespaces: block. The flat format with explicit namespace fields is still accepted on read.
